/**
 * @tripplan/api — HTTP routes, authz, repositories, workers.
 * Handlers and adapters land in follow-up PRs.
 */
export { DOMAIN_PACKAGE } from "@tripplan/domain";

export const API_PACKAGE = "@tripplan/api" as const;
