{
  "name": "@tripplan/web",
  "version": "0.0.0",
  "private": true,
  "description": "TripPlan SPA (React + Vite + Tailwind)",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint ."
  },
  "dependencies": {
    "@tripplan/domain": "workspace:*"
  },
  "devDependencies": {
    "@tripplan/eslint-config": "workspace:*",
    "@tripplan/tsconfig": "workspace:*",
    "eslint": "^9.17.0",
    "typescript": "~5.7.3"
  }
}
